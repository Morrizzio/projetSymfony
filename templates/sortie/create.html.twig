{% extends 'base.html.twig' %}
{% block title %}{% endblock %}


{% block body %}
    {{ form_start(sortieForm) }}
    {{ form_widget(sortieForm) }}
    <label for="villes">Villes</label>
    <select id="villes" name="villes">
        {% for ville in villes %}
            <option value="{{ ville.id }}">{{ ville.nom }}</option>
        {% endfor %}
    </select>
    <input type="submit" value="valider">
    {{ form_end(sortieForm) }}
{% endblock %}

{% block scripts %}
    <script>
        var id_ville;
        $(function() {  $("#villes").change(function() {
            id_ville= $(this).val();
            $.ajax({
                method: "POST",
                url: "{{ path('sortie_recherche_ajax') }}",
                data: {id: id_ville},
                success: function(){
                    alert("envoyé");
                }
            });
        });});
    </script>
    {% for lieu in lieux %}
    {{ lieu.id }}
    {% endfor %}
{% endblock %}









