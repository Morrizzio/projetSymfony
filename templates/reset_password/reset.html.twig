{% extends 'base.html.twig' %}

{% block title %}Retrouvez votre compte{% endblock %}

{% block body %}
    <h1>Retrouvez votre compte</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Réinitialiser</button>
    {{ form_end(resetForm) }}
{% endblock %}
